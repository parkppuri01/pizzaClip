---
/**
 * 인트로(/) — 피자클립 / 피클 선택 화면.
 * 가이드: web/site-renewal/사이트인트로.png
 * 슬레이트블루 배경 + 둥근 크림 네비(JAM+소셜) + 헤드라인 + 두 앱 선택 카드.
 */
import BaseLayout from "../layouts/BaseLayout.astro";
import { PIZZA_HOME } from "../consts.ts";

const apps = [
  {
    href: PIZZA_HOME,
    img: "/img/intro/card-pizza.png",
    name: "피자클립",
    caption: "복사한건 다 기억하는 피자클립",
    aria: "피자클립 — 복사한 건 다 기억하는 맥 클립보드 앱 보러가기",
  },
  {
    href: "/pickle",
    img: "/img/intro/card-pickle.png",
    name: "피클",
    caption: "캡쳐한건 다 보관해주는 피클",
    aria: "피클 — 캡처한 건 다 보관해주는 맥 스크린샷 앱 보러가기",
  },
];
---
<BaseLayout site="intro" bodyClass="intro-body" title={undefined}>
  <section class="intro">
    <div class="intro__hero">
      <h1 class="intro__title">
        <span class="cream">메뉴바 한 칸에 자리잡은</span><br />
        <span class="cream">편리한</span> <span class="dark">작은 도구.</span>
      </h1>
      <p class="intro__sub">완전 무료 / 내 기기에서만 안전하게 / 광고 제로</p>

      <ul class="intro__cards">
        {apps.map((app) => (
          <li>
            <a class="appcard" href={app.href} aria-label={app.aria}>
              <span class="appcard__art">
                <img src={app.img} width="900" height="900" alt={app.name + " 앱 아이콘"} />
              </span>
              <span class="appcard__cap">{app.caption}</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>
</BaseLayout>

<style>
  .intro {
    min-height: calc(100svh - 90px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(2rem, 6vw, 4.5rem) var(--gutter) clamp(3rem, 7vw, 5rem);
  }
  .intro__hero { width: 100%; max-width: 980px; text-align: center; }

  .intro__title {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: clamp(2rem, 7vw, 4.4rem);
    line-height: 1.18;
    letter-spacing: -0.01em;
    color: var(--intro-cream);
  }
  .intro__title .cream { color: var(--intro-cream); }
  .intro__title .dark  { color: var(--intro-dark); }

  .intro__sub {
    margin-top: clamp(1.1rem, 3vw, 1.8rem);
    font-size: clamp(0.9rem, 2.2vw, 1.15rem);
    letter-spacing: 0.01em;
    color: var(--intro-cream);
    opacity: 0.92;
  }

  .intro__cards {
    margin-top: clamp(2.5rem, 7vw, 4.5rem);
    display: flex;
    justify-content: center;
    gap: clamp(1.5rem, 6vw, 4.5rem);
    flex-wrap: wrap;
  }
  .appcard {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(0.9rem, 2.5vw, 1.4rem);
    width: clamp(150px, 38vw, 230px);
  }
  .appcard__art {
    display: block;
    width: 100%;
    border-radius: 26px;
    overflow: hidden;
    box-shadow: 0 16px 34px rgba(16, 33, 56, 0.28);
    transition: transform 0.2s cubic-bezier(0.22, 1, 0.36, 1), box-shadow 0.2s ease;
  }
  .appcard__art img { width: 100%; height: auto; display: block; }
  .appcard__cap {
    font-weight: 600;
    font-size: clamp(0.9rem, 2.3vw, 1.1rem);
    color: var(--intro-cream);
    transition: opacity 0.2s ease;
  }
  .appcard:hover .appcard__art {
    transform: translateY(-8px) scale(1.03);
    box-shadow: 0 24px 44px rgba(16, 33, 56, 0.36);
  }
  .appcard:focus-visible { outline: none; }
  .appcard:focus-visible .appcard__art { outline: 3px solid var(--intro-cream); outline-offset: 4px; }
</style>
