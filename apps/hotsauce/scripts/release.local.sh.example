# Template for local release secrets.
#
# Setup:
#   cp scripts/release.local.sh.example scripts/release.local.sh
#   then fill in your Developer ID identity + notarytool keychain profile name.
#
# release.local.sh is gitignored, so signing/notary details never get committed.
export HOTSAUCE_SIGN_IDENTITY="Developer ID Application: YOUR NAME (YOUR_TEAM_ID)"
export HOTSAUCE_NOTARY_PROFILE="your-notarytool-keychain-profile"
