# Xcode / build
HotSauce.xcodeproj/
build/
build-mas/
DerivedData/
*.xcuserstate

# Local signing identity (see Signing.xcconfig.example / Signing-MAS.xcconfig.example)
Signing.xcconfig
Signing-MAS.xcconfig
scripts/release.local.sh

# macOS
.DS_Store

# Design source files & references (kept locally, not in repo)
샘플_런캣/
내부아이콘/
메뉴바아이콘/
사이트 디자인/
a/
*.pxd
# 앱 아이콘·배너 등 디자인 원본 48MB — 저장소에는 넣지 않는다(루트 handoff 방침).
# 빌드에 실제로 쓰이는 에셋은 HotSauce/Resources/DesignAssets/ 에 따로 있다.
images/

# OMC state
.omc/
